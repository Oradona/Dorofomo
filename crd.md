# CRD - Dorofomo

## 1. 개요

이 문서는 Dorofomo의 구현 현황과 변경/개선 사항을 정리하기 위한 보조 문서이다.
제품 요구사항(PRD)을 바탕으로, 실제 구현 관점에서 확인해야 할 항목과 변경 방향을 기록한다.

## 2. 목표

- 현재 구현 상태를 명확하게 정리한다.
- 문서화 작업(PRD, CRD)을 통해 코드베이스와 문서의 정합성을 유지한다.
- 향후 변경 사항을 우선순위별로 관리한다.

## 3. 타겟 독자

- 코드베이스를 유지보수하는 개발자
- 기능 변경 또는 개선을 검토하는 사람
- 문서 구조를 파악하려는 기여자

## 4. 핵심 구현 항목

### 4.1 타이머

- 시작 / 일시정지 / 리셋 가능
- 집중 모드와 휴식 모드 전환 가능
- 사용자 입력 시간을 기반으로 카운트다운 수행

### 4.2 XP / 레벨

- 집중 세션 완료 시 XP 증가
- XP를 기반으로 레벨 계산
- 진행률을 UI에 반영

### 4.3 ASMR

- 화이트 노이즈 / 비 소리 / 집중 톤 선택 가능
- 볼륨 조절 가능
- off 상태 지원

### 4.4 친구 / 랭킹

- 친구 이름과 XP 저장 가능
- 친구 삭제 가능
- 내 XP와 친구 XP를 합쳐 랭킹 정렬

### 4.5 상태 저장

- localStorage 기반 상태 저장
- 새로고침 후 데이터 복원 가능
- 손상된 저장 데이터는 안전하게 무시

## 5. 현재 구현

현재 Dorofomo는 단일 페이지 정적 웹 앱으로 구성되어 있다.

- `index.html`: UI 구조 정의
- `app.js`: 타이머, XP, 친구, ASMR 로직 포함
- `styles.css`: 스타일 정의
- `README.md`: 기본 실행 방법 안내

## 6. 향후 아이디어

- 저장 데이터 스키마 버전 관리
- 입력값 검증 강화
- 4회 집중 완료 후 긴 휴식 자동 전환
- 통계 화면 추가
- 레벨업 시 시각 효과 추가
- 더 다양한 ASMR 사운드
- 친구 검색/초대 기능
- 테스트 추가

## 7. 비고

- 서버 백엔드, 인증/로그인, 실시간 동기화는 현재 범위 밖이다.
- 이 문서는 현재 Dorofomo 구현을 기준으로 작성된 CRD이다.
- PRD(`prd.md`)와 함께 유지보수한다.
